
<%- include('../partials/header') %>

<% if(student){ %> 
<%- include('../partials/aside') %>
    <main>
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Date</th>
                    <th>Plane</th>
                    <th>Instructor</th>
                    <th>Details</th>
                    <th></th>
                </tr>
                <tbody>
                    <%lessons.forEach((l, idx) => { %>
       
                <tr>
                    <td><%=l.date%></td>
                    <td><%=l.planes.plane%></td>
                    <td><%=l.instructor%></td>
                    <td><%=l.category%></td>
                    <td><a href="lessons/<%=l.id%>/edit">edit</a>
                        <a href="lessons/<%=l.id%>">Details</a>
                        <form action="/lessons/<%=l.id%>?_method=DELETE" method="POST" autocomplete="off"><button type="submit">Delete</button></form>
                    </td>
                </tr>
              
            <%})%>
                </tbody>
            </thead>
        </table>
    </main>    
</div>
<%}else{%>
    <%- include('../partials/landing') %>

<%}%>
<%- include('../partials/footer')%>



