<!-- <div class="container">
    <div>
    <span class="card-title">
        <% if(student.avatar) { %> <img src="<%=student.avatar%>" alt=""> <%}else{%> <i class="small material-icons">perm_identity</i><%}%>
    </div>
  </div> -->




  <% skills.forEach(function(skill,idx) { %>
    <li>
            <form action="/skills/<%=idx%>?_method=DELETE" class="delete-form" method="POST" autocomplete="off" >
                <button type="submit">X</button>
            </form>

        <%= skill.level %>
        &mdash;
        <a href="/skills/<%= idx + 1 %>"><%= skill.name %></a>
        &mdash;
        <%= skill.description %>
    </li>
<% }) %>